# Rowing Navigator ストア掲載・審査資料案

更新日: 2026-07-20

## 基本情報

| 項目 | 案 |
|---|---|
| 正式名称 | Rowing Navigator |
| Bundle ID / Application ID | `jp.kosei.rowingnavigator.tsukuba` |
| 初回version | `1.0.0` |
| 初回build | `1` |
| 主カテゴリ | Sports |
| 副カテゴリ候補 | Navigation |
| 料金 | 無料 |
| 広告 | なし |
| App内課金 | なし |
| 対応言語 | 日本語 |
| サポートURL | **[要入力]** |
| プライバシーポリシーURL | **[要入力]** |
| 問い合わせメール |  university.of.tsukuba.rc@gmail.com|

## Apple subtitle案（30文字以内）

桜川でのローイング安全支援

## Google Play短い説明案（80文字以内）

桜川でのローイング航行を、危険区域・他艇接近の表示と録音音声で支援します。

## 詳細説明案

Rowing Navigatorは、茨城県土浦市の桜川周辺で行うローイング練習の安全確認を支援するアプリです。

主な機能:

- チームの作成、固定招待コードによるチーム参加
- GPSを用いた現在地、速度、進行方向の表示
- 固定危険区域、臨時危険区域、固定流木の表示
- 他艇との接近予測と録音音声による警告
- 同じチームの艇とのリアルタイム位置共有
- チーム員全員による危険区域の追加・編集・削除
- 航行開始時に入力した名前の共有
- コーチ・監視端末での名前、艇位置、長時間停止の確認
- 距離、スプリット、ストロークレートを含む練習記録
- GPX・CSV形式での練習記録出力

初回はチームを作成するか、チーム員から受け取った固定招待コードで参加します。位置と危険区域は同じチームの端末だけで共有します。招待コードに自動期限や自動変更はありません。

利用者が航行を開始してから終了するまで、画面消灯中や別アプリ表示中も接近警告と位置共有を継続します。位置情報の用途は権限要求前にアプリ内で説明し、航行終了時にリアルタイム共有を停止します。停止中の自動追跡は行いません。

本アプリは安全確認を補助するものであり、衝突回避を保証するものではありません。目視、声かけ、救助艇配置、荒天時の練習中止など、所属団体の安全ルールを必ず併用してください。GPS誤差、通信遅延、電池切れ、端末やOSの制約により、警告が出ない、遅れる、停止する場合があります。

## Google Play位置foreground service申告案

公開候補のmerged manifestでは`ACCESS_BACKGROUND_LOCATION`を要求しない。ただし、画面消灯・別アプリ表示中もlocation foreground serviceで位置取得を継続するため、Google Play上はForeground Service申告に加え、Consoleが表示する位置情報申告を省略しない。利用者操作による開始、航行終了時の即時停止、目立つ事前開示、プライバシーポリシー、実演動画を同じ説明で提出する。

### 中心機能

利用者が「航行スタート」を押してから「航行終了」を押すまで、GPS位置を継続取得し、自艇と周囲の艇との接近予測、危険区域への接近警告、艇間位置共有、練習記録を提供します。画面消灯や他アプリ表示で位置更新が停止すると、主要な安全支援機能が成立しません。

### 利用者が開始する操作

1. アプリを開く
2. 初回だけ、チームを作成するか固定招待コードで参加する
3. 「航行スタート」を押し、航行する人の「名前」を入力する
4. 画面消灯・別アプリ表示中の位置取得、同じチームへの共有、停止条件の明示説明を読む
5. 「同意して続ける」を押す
6. OSの位置情報権限と、Android 13以降では航行中通知の権限を選択する
7. 航行を終了する場合は「航行終了」を押す

### 中断された場合の影響

位置情報更新が中断されると、他艇との接近予測、固定危険区域への接近判定、位置共有、監視、航行記録が更新されません。アプリはGPS途絶または警告処理停止を画面と音声で知らせます。

### 実演動画に含める画面

- 航行開始前の画面
- バックグラウンド位置情報の明示説明全文
- OSの権限選択
- 航行中のAndroid foreground service通知
- ホーム画面または画面消灯中も位置共有が更新される様子
- アプリへ戻り「航行終了」で共有が消える様子

## App Store審査メモ案

本アプリは、桜川でのローイング練習中に、固定危険区域と他艇への接近を表示・警告する安全補助アプリです。

審査手順:

1. アプリを起動してください。起動だけでは位置情報権限を要求しません。
2. 初回画面で「招待コードでチームに参加する」を選び、審査用コード **[固定コードを要入力]** を入力してください。
3. 参加後に地図が表示されます。チームボタンから同じ固定コードを確認・コピーできます。
4. 地図上の危険区域編集ボタンを開くと、同じチームの全メンバーが臨時危険区域を追加・編集・削除できます。
5. 地図下部の「航行スタート」をタップしてください。
6. 「名前」を入力し、艇種と端末位置を選びます。「音声を確認する」で録音済み警告音を確認できます。
7. 「航行スタート」をタップすると、バックグラウンド位置情報の明示説明が表示されます。
8. 「同意して続ける」をタップし、OSから求められた場合は位置情報の「Appの使用中」を許可すると航行が開始されます。
9. 画面消灯または他アプリ表示中も、航行中の位置更新と警告処理を継続します。
10. 「航行終了」で位置共有とバックグラウンド追跡を停止します。

メール/パスワードのログイン画面はありません。チーム作成または招待コード参加を利用者が選んだ時だけ、Firebase Authenticationの匿名認証を内部で使用します。一度参加した端末は次回以降、保存済み認証と所属を自動復元して地図へ進みます。アプリデータ消去等で認証が本当に失われた場合だけ、固定招待コードで再参加します。審査用チームと固定コードは審査期間中有効なまま維持します。

固定危険区域は桜川向けデータをアプリに同梱しています。審査地点が対応水域外の場合もアプリは起動・航行できますが、「対応水域の範囲外」または「対応水域が未設定」と表示される場合があります。

本アプリは衝突回避を保証せず、目視確認等を代替しません。この制約はアプリ内とストア説明に表示します。

審査連絡先: **[氏名・電話番号・メールを要入力]**

## スクリーンショット構成

1. チーム参加 — 作成/固定招待コード参加の初回画面
2. メイン地図 — 自艇、同チームの他艇、固定危険区域
3. 接近警告 — 省スペース警告バナーと対象名
4. 航行開始前 — 名前・艇種・端末位置・音声確認
5. 監視 — 地図上の名前、艇一覧、電池残量、長時間停止
6. 練習記録 — 距離、時間、スプリット
7. 危険区域編集 — チーム全員による臨時危険区域と固定流木の調整

撮影時の名前は「テスト1」等の架空名にし、実在の個人名、不要な正確な居場所、Firebase UIDを表示しない。公開候補buildの実画面だけを使用する。

## 公開前の未確定事項

- [ ] 運営主体の正式名称
- [ ] サポートURL
- [ ] プライバシーポリシーURL
- [ ] 問い合わせメール
- [ ] App Store審査用の氏名・電話番号
- [x] 承認済み正式アイコンをiOS/Android/Play用へ適用
- [x] チーム招待制とチーム別Firebase Rulesを実装し、Rules Emulator 13件に合格
- [ ] 審査専用チームを作成し、審査期間中有効な固定コードを入力する
- [ ] 共有危険区域の利用規約、問題報告窓口、管理者対応手順を用意する

## 初版後の技術更新候補

- Flutterが予告しているiOS UIScene lifecycleへ移行する
- AndroidのBuilt-in Kotlin移行に対応したFlutter/plugin構成へ更新する
- `google_maps_flutter_ios`のSwift Package Manager対応状況を確認する

AndroidのBuilt-in Kotlin、iOS UIScene lifecycle、`google_maps_flutter_ios`のSwift Package Manager対応は将来互換の追跡項目であり、現時点の自動テスト/Android Debug buildを止めるエラーではない。
