# 2026-07-30 航路・座標 profile v6 反映記録

## 入力

- changeset: `障害物座標プロット_変更適用/2026-07-30T13-09-49-989Z-INCOMPLETE`
- 入力 JSON: `files/sakuragawa_obstacles.json`
- 入力 SHA-256: `3093ea9623a90a2ca09e0ac3f62caf05b8fa1b03fdb20437f223c20599df3098`
- `Downloads/sakuragawa_obstacles-2.json` は上記入力 JSON とバイト単位で一致した。

## 反映内容

- アプリ同梱 profile を version 6 に更新した。
- 最終 SHA-256: `fca7a4f8146cfd85fdc3fde545f8ac1d22533ad2c778e48df220747dc292455f`
- `shore_bd39c863` は入力と異なっていた4点を反映し、全27点を入力と一致させた。
- `shore_north` 154点、`shore_south` 108点は入力と既存アプリが既に全点一致していたため保持した。
- 霞ヶ浦と桜川河口について、中心線2本と往路・復路の航路4面を追加した。
- 入力の霞ヶ浦航路が存在しない中心線IDを参照していたため、`centerline_kasumikagaura` へ修正した。
- 桜川河口の往路・復路に実際の重なりがあったため、共有境界に約1mの間隔を設けた。
- 航路全頂点を中心線の判定範囲へ収めるため、中心線を最小限延長・補間した。
- 既存の広い `operationalCoveragePolygon` は、入力の狭い範囲へ戻さず保持した。
- 共有校正文書を `fixed_obstacle_calibrations_v6` へ世代分離し、profile version/SHA とローカル Firestore Rules を同期した。

## 意図的に反映しなかったもの

- `bridgefoot_f184b724`（水郷橋橋脚1）は反映していない。入力では橋脚名にもかかわらず `kind: bridge` で、閉じ点も重複していた。`bridgePier` として検証すると約25.4m × 8.9mで、許容長20mを超えるため、安全に自動補正できない。
- 入力 layout は profileVersion 1、無効な中心線参照、上記橋脚候補を含むためアプリへコピーしていない。アプリ実行時の基準データは `assets/data/sakuragawa_obstacles.json` である。

## 検証結果

- プロット検証: error 0。残る warning は `ashoreAreas` 未設定、旧逆走判定の無効化、橋脚未設定の橋に関する既知警告のみ。
- プロットツール: 23/23 tests、production build 成功。
- Flutter関連テスト: 32/32 成功。
- Flutter全体テスト: 673/673 成功。
- `dart analyze lib test`: 問題なし。
- `git diff --check`: 問題なし。
- 生成定数チェックと profile SHA チェック: 成功。
- Firestore/Realtime Database Rules Emulator: 20/20 成功。version 6 の正常 create/update payload も成功。

## 未完了の外部確認

- Firestore Rules の本番デプロイは未実施。ローカルファイルには今回以外の未コミット変更も含まれるため、全差分レビュー後に別作業として行う。
- 署名済みアプリの配布、実機地図表示、実GPS、現地・水上確認は未実施。
- 現地では、4航路の往復方向、境界の隙間、中心線追従、`shore_bd39c863` の水面側、既存岸・橋の表示を確認する。
- `bridgefoot_f184b724` は、親を `bridge_suigo` とした20m以内の実際の橋脚ポリゴンとして再プロットするか、橋本体の基準線として意図したものかを確認する。

## 再確認コマンド

```sh
cd '/Users/gotoukousei/obsidian/My_Obsidian/漕艇部/桜川プロジェクト/桜川アプリ/rowing-navigator-tsukuba'
dart run tool/generate_hazard_constants.dart --check
tool/update_hazard_profile_hash.sh --check
dart analyze lib test
flutter test
```

Rulesを本番へ反映する場合は、まず `git diff -- firestore.rules firebase_rules_test/rules.test.js` で今回以外の差分も含めて確認する。承認後のデプロイコマンドは次のとおり。

```sh
firebase deploy --only firestore:rules --project rowing-navigator-tsukuba
```
