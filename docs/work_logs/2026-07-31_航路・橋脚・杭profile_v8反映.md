# 航路・橋脚・杭 profile v8 反映記録

## 依頼と入力

- 作業日: 2026-07-31
- 入力: `/Users/gotoukousei/obsidian/My_Obsidian/漕艇部/桜川プロジェクト/桜川アプリ/障害物座標プロット_変更適用/2026-07-31T14-43-15-700Z-INCOMPLETE`
- 入力パッケージは `profileVersion: 7`、SHA-256 `ff5513aa355f681afcd83aa406f39601dc45b48068a23b32d2e5c18598fe798c`、検証エラー8件・警告21件を含む `-INCOMPLETE` だった。入力元は変更していない。
- 座標は入力ファイルを基準にし、橋脚の位置を補正・創作していない。

## 判断したデータ修正

### 橋脚

14個の `bridgePier` をアプリへ反映した。親橋との紐付けは次のとおり。

| 親橋 | 橋脚数 |
| --- | ---: |
| 水郷橋 | 3 |
| 常磐線鉄橋 | 4 |
| 桜川橋 | 2 |
| 匂橋 | 2 |
| 銭亀橋 | 2 |
| 土浦橋 | 1 |
| 学園大橋 | 0 |

土浦橋は入力どおり1本だけ反映した。学園大橋は入力ファイルに橋脚が存在しなかったため、座標を推測して追加せず、従来の橋桁警告を維持する未プロット警告を残した。

### 杭

入力では杭1・杭2が一時的に `driftwood` の基準線として保存されていたため、既存の固定流木共有校正へ誤って入らないよう、次のように分類した。

- `driftwood_3eacc519` → `pile_3eacc519`、杭1、外周6点
- `driftwood_e14ff5c6` → `pile_e14ff5c6`、杭2、外周9点
- 閉じた基準線の最後の重複点だけを除去。実座標は変更していない。
- `driftwood_51534e52` は名前・種別が流木のため流木として残した。ただし、既存の共有校正対象は従来の `driftwood_estuary` だけに限定し、追加流木へ同じ校正値を誤適用しないようにした。

杭は `obstacles` の直接外周ポリゴンとして扱う。アプリでは流木と同じ近接6m・単発音対象・低速静音なしの警告軸を使う。杭専用音声は今回支給されていないため、誤って「流木」と読み上げないよう、当面は `generic_warning.mp3` を使う。

## プロットツールの変更

- `ObjectKind` に `pile` を追加。
- 杭を外周ポリゴンとして作成・編集・インポート・エクスポートできるようにした。
- 杭フォルダを追加。
- 既存の橋脚外周ポリゴン方式を維持し、幅入力・矩形自動生成は追加していない。
- 橋脚の長辺上限を20mから30mへ変更。今回の実測外周26.2mなどを誤ってエラーにせず、極端な入力の検出は残した。
- 橋脚と航路中心線が6m以内になる検証は、座標を強制変更させない `warning` へ降格した。アプリの実行時衝突警告は変更していない。

## アプリの変更

- `StaticObstacleKind.pile`、表示ラベル、地図色、警告タイトル、記録表示を追加。
- 杭を静的衝突判定・近接注意・提示状態圧縮のカテゴリとして追加。
- 固定プロフィールを v8 に更新し、SHA-256を次へ固定した。

`9933b7b21e761108b7b0ce2809afd663b438d566e21b69c63afc30259e48be27`

- 共有校正文書を `fixed_obstacle_calibrations_v8` へ分離。
- `shared_safety_calibration.g.dart` と `firestore.rules` の許可IDを生成し直した。

## 検証結果

- `tool/update_hazard_profile_hash.sh --check`: 成功
- `dart run tool/generate_hazard_constants.dart --check`: 成功
- プロットツール `npm test`: 6ファイル・24テスト成功
- Flutter対象テスト: 31件成功（プロフィール読込、橋脚・杭、警告音、提示状態、Firestore契約）
- `dart analyze lib test`: 成功（No issues found）
- Flutter全体テスト: 676件成功。JSONが50KBを超えたときの `rootBundle.loadString` のIsolateデコードがWidgetテストの待機と噛み合わず、設定画面が読み込み中のままになる回帰を確認したため、プロフィール読込をバイト読込＋UTF-8デコードへ変更し、全体テストで再確認した。
- Rules Emulator: Firestore・Realtime Databaseを起動した `firebase emulators:exec --only firestore,database "npm --prefix firebase_rules_test test"` で20/20成功。
- v8の橋脚・杭追加でFirestore Rulesの評価式上限に近づいたため、生成スクリプトを修正し、v8では許可IDと基準線の頂点数をRulesで確認し、橋脚・杭の外周頂点の厳密な個数・値域は生成定数とアプリ読込時に検証する構成にした。旧profile向けの頂点数検証は維持した。
- プロフィール読込テストで、変更後の同梱座標にレベル `error` がないことを確認した。

## 未実施・手動作業

1. `firestore.rules` はローカルファイルを更新しただけで、本番Firebaseへはデプロイしていない。デプロイする場合は、確認後に次を実行する。

   ```bash
   cd "/Users/gotoukousei/obsidian/My_Obsidian/漕艇部/桜川プロジェクト/桜川アプリ/rowing-navigator-tsukuba"
   firebase deploy --only firestore:rules
   ```

2. 本番共有校正を使う場合は、v7の校正をv8へ自動移行しない。v8の座標を基準に、必要な端末校正を新しい `fixed_obstacle_calibrations_v8` へ作成する。
3. 杭専用の読み上げを追加する場合は、WAVマスターを `tool/audio_src/pile_warning.wav` に置き、`tool/normalize_warning_audio.sh` と `tool/check_warning_audio.sh` を実行してから、`warning_audio_config.dart` の暫定フォールバックを差し替える。
4. 学園大橋の橋脚座標は未入力のまま。現地または航空写真で確認できたとき、プロットツールで親橋 `bridge_gakuen` に紐付けて追加する。
5. 実機・水上での橋下通過、杭接近、音声、低速時の挙動は未検証。ローカルテスト成功だけでは本番Rules・署名ビルド・端末・水上安全性を保証しない。
