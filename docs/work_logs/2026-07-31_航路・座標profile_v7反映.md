# 2026-07-31 航路・座標 profile v7 反映記録

## 入力

- 変更適用パッケージ: `../障害物座標プロット_変更適用/2026-07-31T13-22-49-078Z`
- 入力JSON: `/Users/gotoukousei/Downloads/sakuragawa_obstacles-3.json`
- パッケージ内JSON: `障害物座標プロット_変更適用/2026-07-31T13-22-49-078Z/files/sakuragawa_obstacles.json`
- Downloads JSONとパッケージ内JSONはバイト単位で一致した。
- 入力JSONのSHA-256: `2d3db457388c175245509f684e768a78bceaba00ec6c2ff3d30c602ec4993daa`
- 入力のprofile version: 6

## profile世代の判断

現行アプリの同梱profileはすでにversion 6、SHA
`fca7a4f8146cfd85fdc3fde545f8ac1d22533ad2c778e48df220747dc292455f`
だった。今回の入力は同じversion 6を名乗るが、中心線・航路レーン・陸上エリア・座標が変わっている。
そのままversion 6へ上書きすると、旧v6の共有校正ドキュメントを新座標へ誤適用する可能性があるため、アプリ側のコピーだけversion 7へ正規化した。
入力JSONと変更適用パッケージ自体は変更していない。

## 反映内容

- `assets/data/sakuragawa_obstacles.json` を入力JSONの全フィールド・全座標と意味的に一致させ、`version`だけ7へ更新した。
- `assets/data/sakuragawa_obstacles.layout.json` を追加し、`profileVersion`を7へ合わせた。
- 正規化後のprofile SHA-256: `41c44b63977a0dcd3db0617043735ad13bfde1e50e946198b548b4d2aa8f781b`
- `lib/config/hazard_profile_config.dart` をversion 7 / 新SHAへ同期した。
- 共有校正の書き込み先を `fixed_obstacle_calibrations_v7` へ分離した。既存v6文書の読み取り互換はRulesとプロットツールで保持した。
- Firestore Rulesへprofile v7のversion/SHA検証、create/update許可、`validSharedSafetyCalibrationV6`を追加した。
- プロットツールの新規profile既定値を7にし、共有校正の読み取りはv7を最優先にした。
- 固定危険区域の校正対象IDと頂点数は変更なしだったため、生成定数の実体は変わらず、生成器のcheckだけを通した。
- 中心線は2本から3本へ増えた。
  - `centerline_kasumikagaura`: 6点（3点目以降の一部座標を入力へ更新）
  - `centerline_sakuragawa_estuary`: 9点
  - `centerline_sakuragawa_upstream`: 14点を追加
- 航路レーンは4枚から6枚へ増えた。河口レーンの方向を入力に合わせ、上流の往路・復路を追加した。
- `ashoreAreas`に1区域を追加した。
- 既存の固定危険区域14件と案内区域8件は座標・頂点数を変更していない。

## 入力検証

- 変更適用パッケージの検証はerror 0、warning 14。
- warningの内訳はdraft 4件、上流レーンの中心線端点外れ2件、旧reverse区域の無効化1件、橋脚未設定7件。
- 上流レーンの端点外れは、その区間で逆走判定が働かないという意味。座標を推測で延長・補正すると手動プロットの意図を変えるため、自動補正せず入力形状を保持した。
- 独立検査でID重複なし、座標の範囲外なし、0.3m未満の隣接点なし、閉じるべき区域の閉じ点欠落なしを確認した。
- アプリ側JSONは、入力JSONのversionだけを7へ置換したものと意味的に一致した。

## 検証結果

- `dart run tool/generate_hazard_constants.dart --check`: 成功
- `tool/update_hazard_profile_hash.sh --check`: 成功
- プロットツール `npm test`: 23/23成功
- プロットツール `npm run build`: 成功
- profile/校正/Rules/固定区域関連Flutterテスト: 24/24成功
- 中心線・6レーン・逆走・陸上・SafetyOrchestrator関連Flutterテスト: 68/68成功
- `dart analyze lib test`: No issues found
- Rules Emulator（Firestore + Realtime Database）: 20/20成功。v7共有校正の正常create/updateを含む。
- 全Flutterテスト: 598成功、1失敗。失敗は
  `test/screens/fixed_obstacle_warning_settings_screen_test.dart` の
  「島2（上流）は初期状態でオフ、設定画面からオンへ変更できる」で、
  `pumpAndSettle timed out`。今回のprofile v7経路とは無関係の画面テストとして未修正。
- `git diff --check`: 成功

## 未完了の外部確認

- Firestore Rules本番デプロイは未実施。ローカルRulesと本番Rulesは別物なので、差分確認と承認後に別作業で実施する。
- 署名済みアプリのビルド・配布、実機地図表示、実GPS、警告音、現地・水上確認は未実施。
- 実機では、霞ヶ浦・桜川河口・桜川上流の往復方向、上流レーン端点外側の逆走判定停止、岸の水面側/陸側、陸上エリアでの音声停止、旧v6校正がv7へ混入しないことを確認する。

## 再確認コマンド

```sh
cd '/Users/gotoukousei/obsidian/My_Obsidian/漕艇部/桜川プロジェクト/桜川アプリ/rowing-navigator-tsukuba'
dart run tool/generate_hazard_constants.dart --check
tool/update_hazard_profile_hash.sh --check
dart analyze lib test
flutter test --no-pub test/hazard_profile_config_contract_test.dart test/firebase_rules_contract_test.dart test/models/shared_safety_calibration_test.dart test/services/preset_obstacle_service_test.dart test/services/shared_safety_calibration_service_test.dart
cd tool/obstacle-plotter
npm test
npm run build
```

本番Rulesへ反映する場合は、先に今回以外の未コミット差分を確認し、承認後に次を実行する。

```sh
cd '/Users/gotoukousei/obsidian/My_Obsidian/漕艇部/桜川プロジェクト/桜川アプリ/rowing-navigator-tsukuba'
firebase deploy --only firestore:rules --project rowing-navigator-tsukuba
```
