# 改善計画 2026-09-02: `use_navigator.dart` の段階的分解

対象: `lib/hooks/use_navigator.dart`（5,378行）
起点: `cc17b26`（`main`）
再レビュー: 2026-09-04（`7106c52`時点。実測値に変化なし）
前身: 2026-08-20 の検討。成果物は
[重要な不変条件と既存テスト対応表](review/2026-08-20_重要な不変条件と既存テスト対応表.md) と
[check_hotspot_budget.dart](../tool/check_hotspot_budget.dart)。本書がその計画を引き継ぎ、
着手順と対象を改める。以降はこの文書を計画の正本とする。

---

## 0. 再レビューの結論

**大規模な整理は行わない。最初の小さな1PRだけに着手し、その結果を見て次を決める。**

現在の集中は保守上の問題だが、アプリはCIを通り、13個の不変条件の中核も既存テストで
一定程度守られている。したがって緊急の全面改修ではない。一方、ラチェットは全指標で
警告帯に入り、開始・診断・watchdogの状態が1つのフックへ集まっているため、何もしないまま
機能追加を続けることも勧めない。

承認するのはPR1だけとする。PR2以降は、前のPRで機能を保ちつつ、状態の持ち主・依存先・
変更箇所が実際に減った証拠が出た場合に限って進める。

---

## 1. なんのためにやるのか

### 1.1 目的

> **現地で見つかった不具合を直し続けても、[CLAUDE.md](../CLAUDE.md) の
> 13個の不変条件が壊れないようにする。**

そのために、システムを**「一つの変更で理解・確認しなければならない範囲が小さい状態」**へ
スリム化し、保守性を上げる。総行数の削減は目的ではない。責務を説明する型やテストが
増えるため、コードベース全体の行数が一時的に増えてもよい。逆に、ファイルを分けただけで
状態・依存関係・変更箇所が減らないなら、それはスリム化ではない。

このアプリの価値は「警告が鳴るべきときに鳴る」でしか実現しない。
コーチ艇がいない桜川で、2〜12艇が対向艇と十数mですれ違う
（[DESIGN_PRINCIPLES.md](DESIGN_PRINCIPLES.md) 第1章）。
その一点が守られなければ、他の何が良くても意味がない。

警告が鳴らなくなる原因は、大きく次の2つに分けられる。

1. **判定そのものが誤っている。**
2. **判定は正しいのに、配線が壊れている。**
   （評価が回らない・候補が渡らない・音声要求が届かない・
   前回航行の後片付けが今回の状態を消す・開始工程の失敗が全体を止める）

1 の中核は `lib/services/` へ純Dartとして切り出され、`test/services/` の78ファイルで
比較的厚く守られている。`collision_risk_evaluator_service.dart` は1,562行あるが、
責務に対応するテストがあるため、**現時点の優先課題は 1 より 2 である。**
ただし、テストがあることを理由に判定本体を将来の確認対象から永久に外すものではない。

**2 の主要な調整処理が `use_navigator.dart` に集中し、フック全体の配線は直接テスト
されていない。** これがこのリファクタを行う主な理由である。

**不変条件を規範として扱ってよい根拠。** 13項目は `d2b32cb`(2026-07-30)で
一括して文書化されたもので、実装しながら1項目ずつ承認されたものではない。
**2026-09-02 に利用者が全項目を読み、自身の判断と整合することを確認した**ため、
本計画はこれを規範として扱う（CLAUDE.md の同節も参照）。

なお、仮に将来どれかの方針が変わっても、**このリファクタの手順は変わらない。**
リファクタの仕事は振る舞いを変えないことであり、必要なのは
「不変条件が最善であること」ではなく「不変条件が**いまのコードについて事実**であること」
だからである。方針の変更は設計判断であり、実機で確かめて行う別の作業である。

### 1.2 なぜ「増やさない」では解けないのか

設計原則1（機能を止めない）は、能力が欠けたときの**縮退処理**を要求する。
設計原則6（データ欠損は安全の根拠にならない）は、欠けた事実を残す**診断**を要求する。
実際、過去3ヶ月の `use_navigator.dart` への増分（+5,774行 / 20コミット）の大半は、
`fix(diagnostics)` と共有・測位の縮退処理である。

この増加の多くには、実機ログを読んで縮退経路を1つずつ塞いできたという必要な理由がある。
ただし、**必要な機能であることと、現在の実装が最小で重複していないことは別である。**
一括して不要とは扱わず、責務ごとに重複と持ち主を調べる。今後も現地テストのたびに
必要な処理は増えうる。

したがって「これ以上増やさない」は目的に反する。
**必要な増加は許し、増える先を1つのクロージャにしない**ことを対処の中心にする。
ラチェット（`check_hotspot_budget.dart`）は増加を可視化するが、
行き先を作らない限り、いずれ「理由を書いて上限を上げる」しかなくなる。

### 1.3 目的でないもの

次はこのリファクタの目的ではない。判断に迷ったら、これらを理由にしない。

- **行数を減らすこと。** 行数は残高であって目標ではない。
- **見た目をそろえるだけの整理・抽象化。** 保守性を上げない統一は行わない。
- **テストカバレッジの数値。**
- **AIエージェントが読めるようにすること。** 前回これを理由の一つに挙げたが、
  これは目的ではない。正しくは「変更が局所化され、離れた場所の不変条件を
  踏まないこと」であり、人が書いても同じである。

### 1.4 成功をどう測るか

**主指標**（目的に直結する）

- [不変条件×テスト対応表](review/2026-08-20_重要な不変条件と既存テスト対応表.md) の
  **未保護1件・部分的5件**が減ること。
- **1つの安全上の判断を変えるのに、`use_navigator.dart` の何箇所を触るか。**
  直近の実測: 逆走注意の音声トグル1個（`5d73135`）で**12箇所**。
  内訳は state宣言・診断payload 2箇所・setter・開始時のリセット・
  return・`UseNavigator` クラス4箇所。
- 切り出した責務について、**状態の持ち主が1つに決まり、入口と出口がテストで説明できる**こと。
- その責務を変更するときに読む必要のある依存先、可変状態、編集箇所が実際に減ること。
- 削除したコードについて、後述する「削除の証拠」が残ること。

**副指標**（主指標の副産物として下がるはず）

- ラチェット値。下げる方向にしか動かさない。

**測らないもの**: 目標行数と、コードベース全体の純減行数。`useNavigator` が
「状態の公開・コントローラ生成・イベント委譲・UI反映」だけになれば結果は何行でもよい。

---

## 2. いまの状態（2026-09-02 実測、`cc17b26`）

### 2.1 ラチェットは全指標で警告帯に入った

| 指標 | 2026-08-20 基準 | 現在 | 停止上限 | 残余白 |
|---|---:|---:|---:|---:|
| `use_navigator.dart` 行数 | 5,330 | **5,378** | 5,600 | 222 |
| import | 82 | **83** | 86 | 3 |
| useRef | 107 | **108** | 112 | 4 |
| useState | 46 | **47** | 49 | 2 |
| `home_map_screen.dart` 行数 | 2,175 | **2,188** | 2,300 | 112 |
| import | 71 | **72** | 75 | 3 |

**トグル1個をマージしただけで、`use_navigator.dart` の4指標が同時に基準を超えた。**
残りの余白は、同じ規模の機能があと4回で尽きる。ラチェットは設計どおり働いている。
これは全面改修を急ぐ根拠ではないが、**小さく安全な最初の切り出しを始める根拠**にはなる。

### 2.2 ファイルの内訳

| 行数 | 区画 | 性質 |
|---:|---|---|
| 664 | `startNavigation` | 開始工程。try 5・timeout 5・世代確認 7 |
| 561 | `processPosition` | 1Hzの安全判定本体 |
| 403 | `runGpsWatchdogTick` | 復旧・ポーリング・fault昇格の判断 |
| 280 | `handlePosition` / `enqueuePosition` | 測位の入口 |
| 267 | `stopNavigation` | 終了工程。**`attempt()` 手順パターンあり** |
| 267 | 共有安全設定の取得・適用 | |
| 732 | **診断記録の機構**（append群109・`recordSafetyDiagnostics`155・変化検知200・heartbeat 73・起動メタデータ195） | 安全経路の外側 |
| 238 | state宣言（useRef 108 + useState 47 = **153スロット**） | |
| 199 | `return` と `UseNavigator` クラス（**54フィールド**） | |
| 他 | 障害物ロード、受信probe、`buildMyBoat`、lifecycle 等 | |

補助データ: 診断呼び出し **131箇所**、`catch` **28箇所**、コメント行 530。
設計根拠・運用前提・失敗時の意図を持つコメントはコードと一緒に運ぶ。
コードを言い換えただけのコメントは、根拠を確認できた場合だけ削除候補にできる。

### 2.3 決定的な発見: 終了には規律があり、開始にはない

`stopNavigation` は既に**手順パターン**を持っている（4692行〜）。

```dart
Future<bool> attempt(String label, Future<void> Function() operation,
    {Duration? timeout}) async { … }
```

各工程に名前があり、`NavigationStopBudget` で残り時間を配分し、
世代ガードで古い工程の書き込みを弾き、失敗を `navigation_stop_step_failed` として残す。
`NavigationStopBudget` は `lib/hooks/navigation_stop_budget.dart` へ切り出され、
`test/hooks/navigation_stop_budget_test.dart` で固定されている。

**`startNavigation` にはこれがない。** 664行の直列コードに、
`ensureStartIsCurrent()` の呼び出しが7箇所、try/catch が5組、`.timeout()` が5箇所、
それぞれ独自の縮退処理として散っている。

そして不変条件表で**唯一「未保護」なのが不変条件5「音声・危険区域データ・通信の
いずれが欠けても航行開始をブロックしない」**である。これは設計原則1そのものであり、
その履行地点が、規律のない664行の中にある。

**このリファクタの中心はここである。**

---

## 3. 以前の計画から変える点

| 前回 | 今回 | 理由 |
|---|---|---|
| Phase 2: テスト用DIコンテナを先に作る | **撤回** | フック全体をマウントするテストは Firebase・Geolocator・プラットフォームチャネルの偽装が必要で、遅く不安定になる。さらに**モノリスに対するテストはモノリスを固定する**。切り出しを助けず妨げる。時計の注入は、切り出した単位に切り出すときだけ入れる（`estimator_clock.dart` が前例） |
| Phase 3: シナリオテスト5本を先に書く | **撤回** | 5本中「フレーム停止中の音声要求」は `warning_presentation_frame_independence_test.dart` で保護済み。残り4本は対象を切り出せばそのまま単体テストになる。先にモノリス相手に書くと同じ作業を2回やる |
| Phase 4: 触るついでに切り出す（opportunistic） | **専用PRにする** | 現地で見つかった不具合の修正に構造変更を混ぜると、レビューで最も見たい差分が埋もれる。振る舞い不変の専用PRのほうが「移動しただけ」と確認でき、レビューが速い |
| 着手順: 診断 → 開始/終了 → watchdog | 2026-09-02版では開始工程を最初にしたが、**診断の小さい入れ物 → 周辺stateの持ち主 → 開始/終了 → watchdog**へ改める | 開始工程を全行確認すると、続行・中断・非同期化・後片付けが混在していた。「実機で必ず気づく」ことを安全網にはできない。まず外側の小さい責務から持ち主を作り、開始工程へ渡すstateとコールバックを減らす |
| `home_map_screen.dart` は最後 | **`useNavigator`が安定するまでやらない** | 下記 6. と10.6 |

**変えない点**: 大規模な一括リファクタはしない。目標行数を決めない。
`useNavigator` の定義（状態公開・コントローラ生成・イベント委譲・UI反映）を守る。

---

## 4. 計画

各PRは**振る舞いを変えない**。合格条件は 7. に共通で置く。
実装前に、そのPRが触る範囲について「振る舞い保存表」を作る。表には、入力、出力、
状態変更、副作用の順序、待つ処理と待たない処理、timeout、世代確認、失敗時の継続・中断、
診断イベントを記録する。移動後に全行を照合し、表の各行が同じであることを確認する。

### PR1: 診断イベントの入れ物を `SessionDiagnosticBuffer` へ出す

**なぜ最初か。** 診断約732行を一度に1クラスへ移すと、別の巨大クラスを作るだけになる。
最初は、採番・保持上限・開始前保留という小さな一責務だけを切り出す。
警告判断そのものではないため、小さい単位を安全に外へ出す手順を検証できる。
ただし診断は「失敗を見えるようにする」安全機能なので、消してよい付属物とは扱わない。

**やること。**

- `appendDiagnosticEvent` / `appendAlertEvent` / `appendRuntimeDiagnostic` /
  `queuePreSessionDiagnostic` が共有する、seq採番・elapsedMs付与・保持上限・drop計数・
  開始前イベントの保留とflushだけを移す。
- GPS品質・端末の向き・電池・heartbeat・起動メタデータなど、**イベント内容を決める処理は
  このPRでは移さない。** それらまで持たせると役割を一文で説明できなくなるためである。

インタフェースは「完成済みイベントを受けて、順序と上限を守って保持する」だけにする。
経過時間に必要な時計だけを注入し、DIコンテナは作らない。

**固定するテスト。** 代表的なイベントの時刻・type・detailsを変えずに保持できること、
seq採番の単調性、`elapsedMs`、上限到達時のdrop件数、開始前イベントのflush。
イベントカタログと既存ログを読むtoolとの互換性も解析で確認する。

**続行判断。** この小さな切り出しで、診断用stateの持ち主と変更箇所が実際に減らなければ、
「分けただけ」と判断してrevertし、次の切り出し方法を再検討する。

PR1後に続行できる場合の本格的な工程は、10. のロードマップに定める。
先の工程まで準備はするが、前の工程が合格する前に複数PRを並行して実装しない。

---

## 5. 削ぎ落としてよいもの・いけないもの

この計画でいう「スリム化」は、短く書き換えることではなく、責務を適切な持ち主へ移し、
**同じ仕事を二重にしている部分だけを証拠付きで消すこと**である。

### スリム化できたかの判定

切り出し後、次の問いにすべて「はい」と言えることを確認する。1つでも「いいえ」なら、
行数が減っていても採用しない。

1. 新しい部品の役割を一文で説明できる。
2. その役割に関する可変状態の持ち主が1つになっている。
3. 呼び出し側は、内部の手順ではなく入力と結果だけを知ればよい。
4. 同じ変更を行うときに触る箇所・読む依存先が以前より少ない。
5. 値を右から左へ渡すだけの中継層、重複model、汎用DI基盤を増やしていない。
6. 新しい部品を単独で速くテストできる。

テスト・型・振る舞い保存表の追加で総行数が増えても、上の条件を満たせば保守性は向上する。
逆に、総行数だけ減っても条件を満たさなければ、将来の変更コストは下がっていない。

### 削除してよい条件

次のすべてを満たすものだけを削除する。削除内容、根拠、代わりの持ち主、守るテストを
PRの「削除台帳」に1件ずつ書く。

1. 参照元を全件調べ、到達不能か、同じ入力から同じ結果を作る重複だと説明できる。
2. 削除後の責務の持ち主が1つに決まり、入口と出口がテストされる。
3. 失敗時の縮退、再試行、世代ガード、後片付け、診断のいずれも失わない。
4. ログのイベント名・field・schema、保存データ、公開APIとの互換性を壊さない。
5. コメントを削除する場合は、コードを言い換えただけで、設計理由・運用前提・失敗時の意図を
   何も含まないことを確認する。

### 削除してはいけないもの

- 安全側のフォールバック、再試行、世代ガード、部分失敗後の後片付け
- 「失敗が見えない」を防ぐ診断、常設表示、drop計数
- 設計理由を記録したコメント
- 名前や処理が似ているだけで、失敗条件・時間軸・持ち主が違う処理
- テストを通すためだけのコード短縮、1行化、抽象化

行を別ファイルへ移しただけのものは「削除」と数えない。行数の減少は成果の証拠ではなく、
責務の移動または重複除去の結果としてのみ扱う。

## 6. 今回やらないこと

**目的から導いた除外である。**「まだやらない」ではなく「やらない理由がある」。

### `processPosition`（561行）・`applySafetyAssessment`・`buildMyBoat` — 触らない

**現在の証拠では、ここを先に分解する利益より危険が大きい。**
1Hzで全状態を見る必要が実際にあり、`test/services/` と `tool/replay_alerts.dart`、
`test/replay/metamorphic_test.dart` で守られ、最もレビューされている。
目的は不変条件を守ることであって行数を減らすことではないため、Wave A〜Cでは対象外にする。
将来、変更箇所の多さやテスト不能という具体的な問題が出た場合だけ別計画で再評価する。

### `UseNavigator` の戻り値54フィールドの整理 — Wave Eまでやらない

機能追加ごとに3編集点の税を取るのは事実だが、**不変条件を1つも守らない。**
`home_map_screen.dart` の45箇所へ波及する割に、主指標が動かない。
フック本体が小さくなり、この199行が比率として目立ってから再評価する。

### `home_map_screen.dart`（2,188行） — `useNavigator`安定までやらない

13個の不変条件のうちこの画面が担うのは6（地図描画は安全判定用の拡張を反映しない）と
12（受信した提示状態を描画へ渡さない）だけで、**両方とも
`safety_shape_overlay_service` / `map_layer_spec` / models 側へ切り出し済み・保護済み**である。
useRef 6・useState 19 と可変状態も少なく、大半はbuildコード。
ラチェットで増加を止めておけば足りる。

なお 2026-08-20 に codex が指摘したとおり「表示だから壊れても目で分かる」は誤りで、
終了ボタン・警告表示・監視表示は安全運用に関わる。優先度が低いのは
「表示だから」ではなく「担う不変条件が既に別の場所で守られているから」である。

### 撤回済み（3. 参照）

DIコンテナ、フック全体のマウントテスト、
合成GPS列→`SafetySnapshot` のゴールデンテスト。

### 前回から引き続き対象外

`lib/config/` の20ファイル分割、コメント削減、`services/` の統廃合、目標行数の設定。

---

## 7. 各PRの合格条件

1. **振る舞い保存表の全項目が前後で一致する。** とくに処理順、timeout、非同期処理、
   世代確認、縮退、後片付け、診断が同じであること。
2. `flutter test` が通り、既存テストの期待値・assertion・対象ケースを弱めていない。
   import先やテスト用組み立ての機械的変更は許すが、期待値変更は原則として中止条件にする。
3. `dart analyze lib test tool` がクリーン（`tool` を必ず含める。CLAUDE.md 参照）。
4. `bash tool/review/smoke_check.sh` が通る。特に診断イベントカタログと
   鮮度階層（3s / 6s / 30s、停止送信10s）。
5. [quick_review](review_guide/quick_review.md) のF1〜F6について、影響あり・なし・
   実機でのみ確認可能を表にし、影響ありの経路にはテスト根拠を付ける。
6. 診断イベント、保存データ、公開APIの互換性を確認する。意図的な変更が必要なら、
   リファクタPRから分離する。
7. 切り出しで下がった指標だけラチェットを下げる。新しい停止上限は、原則として
   **旧停止上限から実際の減少分を引く**。これにより小変更の余白は残すが、
   切り出した分の余白をもう一度使って元の大きさへ戻せない。
8. 不変条件表を更新し、そのPRで「未保護 / 部分的」がどう動いたかを、直接証明できた
   範囲だけ書く。状態が動かなかった場合も正直に書く。
9. 主指標と削除台帳をPR本文に書く。行数だけを成果として書かない。
10. 1PRを単独でrevertでき、新機能・不具合修正・設定値変更を混ぜていない。
11. 1Hz評価経路や開始経路へ、新しいI/O、待ち時間、Timer、stream購読を足していない。
    移動した場合は個数・順序・timeoutが同じであることを振る舞い保存表で示す。

### マージと現場投入を分ける

CI成功は、実機・バックグラウンド・2台間共有・水上で機能が失われていない証明ではない。
各PRはCIとレビューを満たせば個別にマージできるが、**開始・終了、GPS、警告、通信の
いずれかへ触れた版を現場投入する前**に、人が次を1回確認する。

1. 実機で航行を開始・終了でき、画面更新と航行記録が続く。
2. 音声が鳴る設定で警告音を確認し、画面のsystem fault表示も確認する。
3. 可能なら2台で、互いの位置が表示・更新されることを確認する。
4. バックグラウンド復帰後にGPS、警告、共有が再開することを確認する。

これは将来の実装時に必要な人の確認であり、計画文書を更新する今回の作業では不要である。

## 8. 中止条件

次のいずれかに当たったら、そのPRを中止して報告する。

- 振る舞い不変に収まらないことが分かった（縮退の順序、後片付けの順序、
  警告の出方のいずれかが変わる）。
- 既存テストの期待値・assertion・対象ケースを弱めないと通らない。
- 切り出し先が結局同じ153スロットを参照し、呼び出し側が薄くならない。
- 1つのPRで複数の責務、設定値変更、不具合修正まで必要になった。
- 削除する処理がフォールバック・診断・後片付けではないと証明できない。
- 「現在どう動くか」と「本来どう動くべきか」が食い違い、利用者の判断が必要になった。

## 9. 進め方

新機能開発は止めない。ただしロードマップの各変更は**振る舞い不変の専用PR**として立て、
現地の不具合修正とは混ぜない。1PRずつ、前のPRがマージされてから次に入る。
各PRの後に、主指標、不変条件表、F1〜F6、削除台帳、レビュー負荷を見て、次へ進む利益が
残っているかを判断する。ロードマップの完走自体を目標にせず、危険が利益を上回れば止める。

---

## 10. PR1後の本格スリム化ロードマップ

### 10.1 優先順位の根拠

2026-09-04時点の大きなファイルを、行数だけでなく変更頻度、可変状態、直接テストで比べた。

| 対象 | 規模と最近の変更 | 状態・依存 | 直接テスト | 判断 |
|---|---|---|---|---|
| `use_navigator.dart` | 5,378行・21コミット | import 83、useRef 108、useState 47 | フック全体は無し | **最優先。** 状態と副作用の持ち主を順に外へ出す |
| `home_map_screen.dart` | 2,188行・24コミット | import 72、useRef 6、useState 19 | 画面全体は無し | useNavigator後。まず重要操作のwidgetテスト |
| `use_alert.dart` | 1,366行・3コミット | import 6、useRef 24、useState 3 | command queue / cueあり | 音声経路なので急いで触らない。変更需要が出たときだけ |
| `safety_orchestrator.dart` | 1,669行・8コミット | 純Dart、状態hook無し | 直接テストあり | 行数を理由に分割しない |
| `collision_risk_evaluator_service.dart` | 1,562行・7コミット | 純Dart、状態hook無し | 直接テストあり | 行数を理由に分割しない |
| 記録・校正画面 | 1,059〜1,185行・各3〜4コミット | 状態は比較的少ない | 画面テストあり | 困りごとの証拠が出るまで後回し |

この表は固定順位ではない。変更頻度、事故、テスト状況が変わったら更新する。

### 10.2 Wave A: `useNavigator` の外側から状態の持ち主を作る

PR1の次は、調整役そのものを分解する前に、単独で説明できる周辺責務を1PRずつ外へ出す。

1. **セッション保存キュー。** `buildSessionSnapshot`、`queueSessionWrite`、
   `waitForSessionWrites`、`checkpointSessionIfDue`のうち、書込の直列化・最新pending・drainを
   1つの持ち主へ寄せる。スナップショットの内容決定は別責務として残す。
   書込失敗後も次回保存と安全経路が続くこと、停止時にdrainできることを固定する。
2. **共有安全設定の同期。** 取得・cache縮退・revision監視・利用者確認後の適用をまとめる。
   障害物生成や警告時間そのものは持たせない。圏外時の開始継続、古いrevisionの無視、
   航行中に勝手に形状を差し替えないことを固定する。
3. **残る診断生成の再評価。** GPS品質、向き、電池、heartbeatを一括クラスへ移さない。
   同じ入力snapshotと同じ更新周期を共有するものだけを候補にし、まとまらなければ残す。

Wave Aの終了条件は、フックの行数ではなく、保存・設定・診断の各stateを変更するときに
読む範囲が分離され、各部品を単独テストできることである。

### 10.3 Wave B: 通信の「開始・再試行・監視」を分ける

通信は自艇が他艇から消えるF2と、他艇が見えなくなるF3に直結するため、送信と受信を混ぜない。

1. **送信ライフサイクル。** `startPublishingSetupInBackground`と
   `retryPublishingSetup`を、setup・ACK・再試行時刻・失敗表示の持ち主へ寄せる。
   航行開始を待たせないこと、古い世代の成功が現在stateへ書き込まないこと、再試行上限と
   診断イベントを固定する。位置payloadの中身や送信間隔は変えない。
2. **受信ライフサイクル。** membership self-check、receive access probe、購読開始・停止を
   1つずつ境界化する。`OtherBoatTrackStore`や鮮度3/6/30秒は変えない。
   probe失敗を「他艇なし」と扱わないこと、再購読中も既存艇を即消去しないことを固定する。
3. **送受信の結合確認。** 停止中10秒送信でも30秒の失効前に更新されること、
   受信不能時は2秒送信を維持することを契約テストで結ぶ。

Wave BではFirebaseの実装を1つの汎用通信クラスへまとめない。送信失敗と受信失敗は、
利用者への意味、再試行、診断が違うためである。

### 10.4 Wave C: 開始・終了・watchdogを薄い調整役にする

Wave A・Bで各工程の持ち主ができた後に、現在の長い手順を整理する。

1. **開始工程。** 現在の664行を工程表にし、state変更、`await`/非同期、timeout、7箇所の
   世代確認、失敗時の続行・中断、後片付けを保存する。実行器に大量のsetterが必要なら、
   まだ早いので中止する。不変条件5との不一致を見つけても、修正は別PRにする。
2. **終了工程。** 既存の`NavigationStopBudget`、工程別失敗記録、部分失敗後の続行を保つ。
   開始工程との共通化は、名前が似ているという理由では行わない。時間配分と失敗時の意味が
   同じ部分だけに限定する。
3. **GPS watchdog。** 復旧、単発測位、system fault昇格の判断を純粋な決定へ出し、
   Timer・Geolocator・state更新はフック側に残す。`buildSystemCandidates`は同じsnapshotで
   説明できる場合だけ同じPRに含める。

Wave Cの終了時に、`useNavigator`は「各部品を作る・イベントを渡す・結果をUI stateへ反映する」
調整役に近づく。ここで十分保守しやすければ、以降は止めてよい。

### 10.5 Wave D: GPS入口と1Hz評価は、最後に必要性を再判定する

1. `handlePosition` / `enqueuePosition`について、世代、到着順、重複、backpressureの契約を
   先にテストする。入力キューの持ち主を分けることで状態参照が減る場合だけ切り出す。
2. `processPosition`、`applySafetyAssessment`、`buildMyBoat`は自動的な次工程にしない。
   Wave A〜C後も変更箇所が多く、単体テスト不能という具体的な問題が残る場合だけ、
   合成入力から`SafetySnapshot`までのシナリオテストを先に設計して別計画を作る。
3. 警告判定、提示順、1Hz評価、送信間隔は同じPRで変更しない。

### 10.6 Wave E: UI境界を整理する

`useNavigator`側が安定してから`home_map_screen.dart`へ進む。順序を逆にすると、UIとフックの
両方を同時に直すことになる。

1. 開始・終了ボタン、警告表示、system fault表示について、利用者操作から表示までの
   widgetテストを先に追加する。
2. 2,188行のbuildを、地図、状態表示、設定、開始・終了操作という**見た目の責務**ごとに
   既存widgetsへ寄せる。安全判断や新しいview modelは画面側へ作らない。
3. `UseNavigator`の54フィールドは、実際の利用箇所がまとまった場合だけ安定した小さな
   読み取り専用view stateへ束ねる。元の54項目をそのまま写した巨大modelは作らない。
4. `use_alert.dart`は変更頻度が低く、queue/cueのテストもある。音声不具合の修正需要が
   出たときに、既存のcommand queue・cue admissionという境界に沿って移動する。

### 10.7 Wave F: システム全体の不要物を証拠付きで除く

責務分離と別に、AIコーディングで生じやすい不要物を棚卸しする。1PRに混ぜず、種類ごとに行う。

- 到達不能なファイル・関数・feature flag
- 同じ値を別名で写すだけのmodel、mapper、wrapper
- 同じ設定値・timeout・診断fieldの重複定義
- 実装を言い換えただけで設計理由を持たないコメント
- manifestから参照されるだけ、または将来用に残されたasset

解析上の未参照だけでは削除しない。reflection、platform登録、Firebase schema、保存済みログ、
旧versionとの互換、asset manifestを確認する。`smoke_check.sh`が挙げる未参照音声5件も候補に
すぎず、過去versionや手動試験で不要と確認できるまでは削除しない。

### 10.8 各Waveをすぐ始めるための準備票

次のPRへ入る前に、担当者は以下を1ページに埋める。空欄があれば実装を始めない。

| 項目 | 記入内容 |
|---|---|
| 一文の責務 | この部品がすること / しないこと |
| 現在の持ち主 | 関数、state、Timer、subscription、保存先 |
| 振る舞い保存表 | 入出力、順序、timeout、世代、失敗、診断 |
| 守る不変条件 | 13項目とF1〜F6の該当箇所 |
| 自動テスト | 成功、部分失敗、古い世代、停止・再開 |
| 削除台帳 | 消すもの、代わりの持ち主、削除の証拠 |
| スリム化の証拠 | 減るstate所有者、依存先、編集箇所 |
| 戻し方 | 単独revertで戻ること |
| 現場投入前確認 | 実機1台、2台共有、background、水上の必要範囲 |

この準備票によって、後半の計画はすぐ実装に移せる一方、計画時点の思い込みで不要な抽象化を
量産しない。各Waveは「予定」ではなく「前工程の証拠を受けて開くゲート」として扱う。
