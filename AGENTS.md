# AIエージェントのGitHub運用規約

このリポジトリでは、実装作業の大半を AI エージェントが行う。したがって、
エージェントは実装の完了した検証可能な単位を、利用者を毎回待たせずに
コミット・作業ブランチへ push する。安全性と追跡可能性を優先し、未検証の
混在差分を一括で保存・公開してはいけない。

この規約は Git の履歴と GitHub の連携に関する規約である。Firebase Rules の
本番反映、App Store / Play への提出、署名、外部サービスの設定変更は別の明示的な
依頼を必要とする。コミット、CI 成功、デプロイ、実機確認、水上確認は別の証拠である。

## 基本方針

1. **一つの目的を果たす最小の検証可能な単位を1コミットにする。**
   例: 警告ロジックの変更と、それを守る単体テスト。障害物 profile の変更と、
   JSON/layout、version/hash/生成物、Rules、対応テストを必要に応じて同じ単位にする。
2. **実装を完了し、必要な検証を通したら、エージェントは自律的にコミットする。**
   探索だけ、未完了の試作、失敗している修正、検証不能な変更はコミットしない。
3. **コミットはローカルの保存点、push はGitHubへの送信、PRのマージはmainへの統合である。**
   それぞれを混同しない。
4. **`main` は常に統合候補として保つ。** アプリ・Rules・データ・CIに関わる変更は
   原則として作業ブランチで push し、PRを作る。必要な検証とPRの必須チェックが
   成功し、競合・未解決のレビュー指摘・秘密情報・外部設定変更がなければ、エージェントは
   利用者の追加確認を待たずに PR を `main` へマージする。

## 作業開始時の必須確認

1. リポジトリ直下（`桜川アプリ/rowing-navigator-tsukuba`）で
   `git status --short --branch` を確認する。
2. 既にある変更は、担当タスクとの対応を差分で確認できない限り他者・他タスクの
   作業として扱う。勝手に編集、ステージ、コミット、削除、退避してはいけない。
3. 作業ツリーがクリーンなら、実装前に `agent/YYYY-MM-DD-短いテーマ` 形式の作業
   ブランチを作る。既存の未コミット変更があるときは、勝手にブランチを切り替えず、
   その変更と明確に区別できる範囲でのみ作業する。
4. `git reset`、`git clean`、強制 checkout、履歴改変、他者のコミットの revert は、
   明示的な依頼なしに行わない。

## 自律コミットの条件

以下をすべて満たす「区切り」で、エージェントはコミットまで実行する。

- 依頼された一つの機能、修正、または文書更新が完了している。
- ステージ対象がその目的に必要なファイルだけであり、無関係な既存差分を含まない。
- 変更の種類に応じた検証が成功している。小さな文書変更は差分確認、通常のコードは
  対象テストと解析、安全経路は関連する回帰テスト、Rules は Emulator テスト、
  profile/プロッタは生成・検証・対応テストを行う。
- `git diff --cached --check` が成功し、ステージ済み差分を読んで意図どおりである。
- 秘密情報、生成キャッシュ、個人情報、端末ログ、署名ファイルを含まないことを確認した。

一つの依頼に独立した複数の完了単位がある場合は、各単位で順にコミットしてよい。
ただし、単独では動かない中間状態をコミットしてはいけない。

## ステージ・コミット・pushの手順

1. ファイルを明示して `git add <file> ...` する。**`git add .`、`git add -A` は禁止。**
2. `git diff --cached --check`、`git diff --cached --stat`、必要なら
   `git diff --cached` で対象と内容を確認する。
3. コミットメッセージは `種別(範囲): 内容` の一行で、現在形・具体的な内容にする。
   例: `fix(safety): 停止解除後に固定障害物警告を再武装する`、
   `test(rules): profile v8の読取契約を検証する`、`docs: 危険区域更新の記録を追加する`。
4. コミット後に、実行した検証、コミットSHA、push先ブランチを作業報告に記す。
5. 作業ブランチへ自律的に push し、変更がアプリ・Rules・データ・CIに及ぶ場合は PR を
   作成する。PR本文には目的、検証結果、未検証の外部ゲートを分けて書く。

## 失敗・待機・確認の扱い

- 自分の変更に起因するテスト・解析・CIの失敗は、依頼範囲で修正し、影響する検証を再実行する。修正可能な失敗だけを理由に利用者待ちにしない。
- CI実行中は、適切な間隔で完了を確認する。別途成功済みの同一内容の検証を理由なく繰り返さない。
- 既存の失敗や競合でも、依頼範囲と所有権が明確で安全に解消できるものは進める。所有者不明の差分、未解決の安全判断、必須レビュー不足、保護ルール上の障害は、該当するコミット・push・マージを保留し、独立した作業は続ける。
- 秘密情報、個人情報、端末ログ、署名ファイルの混入が疑われる場合は公開前に確認し、除外できなければ停止する。
- 本番Firebase、課金、認証方式、公開範囲、署名、ストア申請などの外部設定・公開操作は、別途明示的に許可された範囲でのみ行う。ローカル実装や検証まで止める理由にはしない。
- 「実装しない」「コミットしない」「提案のみ」などのユーザー指定を守る。

## 安全機能・危険区域データの追加条件

警告ロジック、航路、障害物、共有、Rulesを変更する前に `CLAUDE.md` と
`docs/DESIGN_PRINCIPLES.md` を読む。変更は、データ、アプリ実装、生成物、Rules、テスト、
必要なら作業記録の整合を確認してからコミットする。CIの成功だけで本番Rules、署名済み
ビルド、実機、または水上での安全性が確認されたとは書かない。

## レビューを依頼されたとき

依頼された対象と完了条件で範囲を決める。資料や設定のレビューを、アプリ全体の動作監査へ拡張しない。

- **差分・限定レビュー**：指定されたファイル・差分と、影響する呼出元・契約・境界を確認する。必要な関連検証だけ実施する。全体用smoke checkは必須ではない。
- **動作確認**：「ちゃんと動くか」「実機テスト前」などの機能監査は `docs/review_guide/README.md` と `quick_review.md` の該当項目を使う。
- **総合レビュー**：明示的に全体監査を依頼された場合、または影響範囲から全体確認が必要と判断できる場合に `docs/review_guide/README.md` の総合版を使う。依頼範囲を大きく広げる場合だけ確認する。
- 衝突警告の監査は `.claude/skills/collision-safety-review/SKILL.md` から、差分・総合の該当モードを選ぶ。

レビューのみの依頼では指摘・根拠・修正案を報告し、コードを変更しない。「確認して直す」など修正も許可されている場合は、確認した問題を依頼範囲で修正・検証する。
見ていないものを確認済みとしない。レビューだけの依頼でアプリのビルド・起動・署名・配布は実行しない。

## 人が行うこと

通常の実装コミット・作業ブランチへのpush・PR作成・PRのmainへのマージは、エージェントが
行う。利用者が行うのは、Firebase Rulesの本番反映、署名、ストア提出、配布、公開などの
外部サービス管理画面での承認操作である。エージェントは、マージを完了した後にPR番号、
マージコミット、検証結果、未検証の外部ゲートを日本語で報告する。マージ条件を満たせない
場合は、理由と利用者が必要な操作を具体的に案内して止まる。
